import { DocConfig } from './interface/doc.interface';

export const SWAGGER_CONFIG: DocConfig = {
  title: 'NestJS Movies',
  description: 'API desenvolvida com foco no gerenciamento de filmes.',
  version: '1.0',
};
